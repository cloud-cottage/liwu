# @liwu/server

Node 后端骨架，采用 TypeScript 导向，待选定运行时与模块划分后扩展。

## 当前状态

脚手架阶段，`dev` 与 `build` 脚本尚未接入实际运行时。

## 命令

```bash
# 在仓库根目录
npm run build:server
```

## 规划

后续可在此承载：

- 统一 API 网关
- 管理端专用接口
- 定时任务与消息队列消费者